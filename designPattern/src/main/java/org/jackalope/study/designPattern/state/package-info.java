/**
 *
 * 状态模式：
 * 允许一个对象在其内部状态改变时改变它的行为；
 * 包括：
 * 使用环境角色；
 * 抽象状态角色；
 * 具体状态角色；
 *
 * 区分策略模式与状态模式的关键在于：由算法驱动还是由状态驱动；
 * “State模式”的状态是在对象内部的；
 * “Strategy模式”的算法可以是在对象外部的；
 *
 * @author zowie
 * Email: nicholas@iuni.com
 */
package org.jackalope.study.designPattern.state;